package main

func main() {
	// atomic
	/*
		golang memiliki package sync/atomic
		atomic = package yg digunakan untuk menggunakan data primitive secara aman pada proses concurrent
		contohnya sebelumnya kita telah menggunakan mutex untuk melakukan locking ketika ingin menaikan angka di counter. hal ini sebenarnya bisa digunakan dengan atomic package
		ada banyak sekali func di atomic package, silahkan explore di dokumentasinya
	*/
}
