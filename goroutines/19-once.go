package main

func main() {
	// once (sync.Once())
	/*
		once = fitur di golang yang bisa digunakan untuk memastikan bahwa sebuah func di eksekusi hanya sekali
		jadi berapa banyakpun goroutine yg mengakses, bisa dipastikan bahwa goroutine pertama yang bisa mengeksekusi func nya
		goroutine lain akan dihiraukan, artinya func tidak akan dieksekusi lagi
	*/
}
